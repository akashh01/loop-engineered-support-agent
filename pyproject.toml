[project]
name = "support-loop-demo"
version = "0.1.0"
description = "Loop engineering demo: a self-learning customer support agent"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "langgraph>=0.2",
    "chromadb>=0.5",
]

[project.optional-dependencies]
claude = [
    "anthropic>=0.40",
]

[tool.uv]
package = false
